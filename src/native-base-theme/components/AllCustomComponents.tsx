// import { styleRule as MasterPageStyle } from '../../Components/Shared/Layouts/MasterPage/MasterPage.style';
import variable from '../variables/platform';

export const styles = (variables = variable) => {
    return Object.assign({}
        // MasterPageStyle(variables),
    )
}
